{
    "version": 2,
    "builds": [
        {
            "src": "app/api/server.py",
            "use": "@vercel/python",
            "config": {
                "maxLambdaSize": "50mb"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.py"
        }
    ]
}